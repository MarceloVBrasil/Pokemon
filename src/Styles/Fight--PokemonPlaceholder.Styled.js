import styled from "styled-components";
import pokeballPlaceholder from '../images/pokeball.png'

export const PokemonPlaceholder = styled.div`
    background-image: url(${pokeballPlaceholder});
    background-size: contain;
`